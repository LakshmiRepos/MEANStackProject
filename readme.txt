    Extract this project into specific folder.
    Install node, npm and MongoDB in local Machine
    Navigate to root of the project, then run following command
    > npm install(To install all node dependency)
    Import products list to mongoDB by running following command.
    > mongoimport -d shoppingCart -c products --file products.bson
    Once you installed all dependencies, run npm start to run node app.
